<h2><%= @project.name %></h2>

<p><%= @project.technologies %></p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Delete',
            @project,
            method: :delete,
            data: {confirm: 'Are you sure?'}
%>
