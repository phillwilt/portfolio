%h1 Thoughts

<br />

- if policy(Article).new?
  = link_to 'New Thought', new_article_path, remote: true, class: 'button', id: "new-article-link"

#articles
  - @articles.reverse.each do |article|
    = render 'articles/article', article: article
